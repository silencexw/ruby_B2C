<!DOCTYPE html>
<html>
<head>
  <title>Dropdown Menu Example</title>
  <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
</head>
<body>
<div class="container mt-4">
  <ul class="list-group" id="categoryList">
    <% @categories.each do |group| %>
      <div class="dropdown">
        <button class="btn btn-secondary dropdown-toggle" type="button" id="categoryDropdown<%= group.first.id %>" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
          <%= group.first.title %>
        </button>
        <div class="dropdown-menu" aria-labelledby="categoryDropdown<%= group.first.id %>">
          <% group.last.each do |sub_category| %>
            <a class="dropdown-item" href="<%= category_path(sub_category) %>"><%= sub_category.title %></a>
          <% end -%>
        </div>
      </div>
    <% end -%>
  </ul>
</div>

<script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
<script src="https://cdn.jsdelivr.net/npm/popper.js@1.16.0/dist/umd/popper.min.js"></script>
<script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body>
</html>

<style>
    .dropdown-menu {
        position: absolute;
        top: 100%;
        left: 0;
        display: none;
    }

    .dropdown.show .dropdown-menu {
        display: block;
    }

    .list-group {
        position: relative;
        z-index: 1;
    }
</style>
